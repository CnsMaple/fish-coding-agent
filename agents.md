# Agent Rules

## 修改前

- 优先查看相关文件和上下文，理解代码风格和项目结构。

## 修改后

- 每次修改后必须执行以下检查，确保没有错误和警告：
  1. `cargo check`
  2. `cargo clippy`
  3. `cargo fmt -- --check`（如格式化检查失败，先 `cargo fmt` 再重新检查）
- 只有在上述命令全部通过后，才认为本次修改完成。
